import { storiesOf } from '@storybook/react';
import { FieldsListInvite } from './fields_list_invite';

storiesOf('Fields List Invite', module).add('Fields List Invite', () => {
  return <FieldsListInvite />;
});
