import { storiesOf } from '@storybook/react';
import SelectForm from './select';

storiesOf('SelectForm', module).add('Select form', () => {
  return <SelectForm attribute='' name='' value='' />;
});
